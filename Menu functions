// Function to display the contents of a text file
void showMenu() {
    ifstream file("menu.txt");
    if (!file) {
        cerr << "Unable to open menu.txt" << endl;
        return;
    }

    string line;
    while (getline(file, line)) {
        cout << line << endl;
    }

    file.close();
}

int main() {
    showMenu();
    return 0;
}

///////////////////////////////////////////////////////////////////////// 
To read data from a file and store it in an array in C++, you can follow these steps:

Include Necessary Headers: Include the necessary header files (<iostream> and <fstream>).
Declare the Array: Declare an array to store the data read from the file.
Open the File: Open the file using an input file stream (ifstream).
Read Data from the File: Use a loop to read data from the file and store it in the array.
Close the File: Close the file stream after reading the data.
//////////////////////////////////////////////////////////////////////////

#include <iostream>
#include <fstream> // For file handling
#include <string>

using namespace std;

const int MAX_ITEMS = 10;

int main() {
    // Declare an array to store the data
    string data[MAX_ITEMS];
    int numItems = 0; // Variable to keep track of the number of items read

    // Open the file
    ifstream file("data.txt");
    if (!file) {
        cerr << "Unable to open file" << endl;
        return 1;
    }

    // Read data from the file and store it in the array
    string line;
    while (getline(file, line) && numItems < MAX_ITEMS) {
        data[numItems++] = line;
    }

    // Close the file
    file.close();

    // Display the data stored in the array
    cout << "Data read from file:" << endl;
    for (int i = 0; i < numItems; ++i) {
        cout << data[i] << endl;
    }

    return 0;
}
